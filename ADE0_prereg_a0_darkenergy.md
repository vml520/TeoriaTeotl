# ADE0 Pre-registration — the a₀–dark-energy consistency (falsifiable edge)

**Date:** 2026-07-12 (before computing). First open classical/cosmological
target. Sharpens the a₀↔dark-energy link flagged in the handoff (§17.3) and the
~1.7σ wₐ tension from verify_a0_de_consistency.py.

**The claim under test.** TFT says the galactic MOND scale a₀ and dark energy
are ONE ultralight field — the S¹ **phase** field with the sine-Gordon cosine
potential V ∝ (1−cos(φ/f)). That is a **pseudo-Nambu-Goldstone (thawing)
quintessence**: an ordinary scalar, so **w ≥ −1 always — it cannot cross into
phantom (w<−1)**. The same field mass m ~ H₀ that gives a₀ = cH₀/2π (the 2π
from the S¹) makes it *just thawing now*. So ONE parameter (m ~ H₀) sets BOTH
a₀ and the dark-energy equation of state.

## Computation

1. **Solve the thawing quintessence [computed].** Integrate the background
   (matter + pNGB field) from a≈10⁻³ to today; tune the field scale to
   Ω_φ0 ≈ 0.69 and the initial position to the observed w₀ ≈ −0.88. Extract
   w(a), fit CPL w = w₀ + wₐ(1−a); record w_min (must be ≥ −1).
2. **The prediction [derived]:** TFT gives (w₀, wₐ) on the **thawing track**
   (wₐ mild and *positive-definite-kinetic* → w ≥ −1), and **no phantom
   crossing** — a hard, falsifiable feature.
3. **a₀ consistency [derived]:** the field mass today m² = V″(φ_now) ~ H₀²
   (just thawing) — the SAME scale that gives a₀ = cH₀/2π. Verify m ~ O(H₀).
4. **vs DESI [comparison]:** DESI's w₀wₐCDM fit (w₀ ≈ −0.83, wₐ ≈ −0.7) implies
   w(a) = w₀+wₐ(1−a) < −1 in the past — a **phantom crossing** thawing
   quintessence cannot produce. Quantify TFT-wₐ vs DESI-wₐ.

## Gate

- **PASS (a sharp falsifiable prediction)** = TFT predicts a thawing-track
  (w₀,wₐ) with w ≥ −1 (no phantom), a₀-consistent (m ~ H₀), and the DESI
  contrast is stated as a clean falsifier: phantom crossing confirmed ⇒ TFT DE
  falsified; relaxes to the thawing track ⇒ confirmed.
- **FAIL** = the a₀-consistent field does NOT give sensible thawing DE, or the
  prediction is not falsifiable.

**Honest prior & scope.** PASS expected — the value is a SHARP, near-term
falsifiable prediction (no-phantom, small wₐ), not a fit. The tension with
DESI's central wₐ is real and is the point: TFT stakes out the thawing side.
Floors, flagged: the absolute Λ scale (|Λ_cc| / the coincidence problem, why
Ω_φ ~ Ω_m now); the a₀ coefficient's exact value; H₀ itself. No tuning beyond
matching the two measured inputs (Ω_φ0, w₀) — wₐ and the no-phantom feature are
outputs.
