# M0 Pre-registration — the mass-hierarchy interference program

**Date:** 2026-07-11 (written BEFORE any M-stage number is computed)

**Hypothesis H-MASS (PROPOSED):** the lepton mass hierarchy is
near-destructive interference. In the three-phase (circulant) picture the
three sqrt-masses are one scale M times interference factors
1 + sqrt2 cos(delta + 2pi k/3). This has an EXACT ZERO at delta = 3pi/4:
one sibling becomes massless. Nature sits close to that point — so the
muon and tau are not heavy; the ELECTRON is anomalously light, and the
entire hierarchy is controlled by ONE small angle offset epsilon.

**Inputs:** PDG lepton masses only. A = sqrt2 is taken from the closed
Koide arc as an OBSERVED property (its derivation stays parked with its
standing acceptance criterion; this program does not re-litigate it).

**No-tuning / no-numerology clause:** no scanning of constant tables for
epsilon. The single known landmark in the literature (Brannen-type
2/9-radian parametrization) may be REPORTED as a known coincidence,
labeled numerology-class, and not used in any derivation.

## Stages and gates

**M1 — pin the cancellation point (characterization).**
Gates: (a) the near-cancellation restatement reproduces all three PDG
masses exactly (5 sig figs) with the single angle epsilon; (b) the
cancellation point acquires at least one DERIVED physical name (candidate
names to evaluate: singularity/rank-degeneracy of the generation matrix;
chiral-protection point; a symmetry of the ring configuration). PASS =
(a) and at least one derived name in (b). This stage is characterization;
it cannot by itself establish a mechanism.

**M2 — build the interfering object (MAKE-OR-BREAK; not run under M0+M1
authorization).** PASS = a concrete TFT field configuration with two
coherent components (symmetric core + rotating internal mode) whose three
Z3 orientations have energies proportional to |1 + A e^{i(delta+2pik/3)}|^2
— i.e., a structure where amplitudes ADD before squaring into energy.
FAIL = no such coherent-amplitude structure exists in TFT → H-MASS is
demoted to a description (as Koide's relation always was) and the program
stops there honestly.

**M3 — what sets epsilon (only if M2 passes).** PASS = a mechanism making
epsilon small-but-nonzero (near-exact symmetry + identified small
breaking); FAIL = epsilon remains a free parameter.

**M4 — the weak-force link (only if M2 passes).** PASS = the cancellation
point coincides with a helicity/winding-reversal structure of the TFT
object (connecting to the derived winding-line chirality arc); FAIL = no
link found.

**Falsifier for H-MASS as a whole:** M2 failing. Interference requires
coherent amplitude addition; if TFT cannot supply it, the near-zero
reading remains numerically true but physically unexplained, and that
verdict gets logged.
